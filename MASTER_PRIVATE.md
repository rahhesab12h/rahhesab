# راه‌حساب — MASTER PRIVATE

PROJECT=RahHesab
PATH=~/rahhesab
APP_ID=ir.rahhesab.app
BRANCH=main
WEB=www/index.html

RULES:
- پروژه موجود را ادامه بده؛ بازسازی از صفر ممنوع.
- هر مرحله فقط یک دستور Termux؛ منتظر خروجی بمان.
- ADB استفاده نشود.
- قبل از تغییر، وضعیت فعلی بررسی شود.
- بعد از هر تغییر: git diff --check
- اطلاعات حساس مثل keystore و رمزها وارد Git نشوند.

CURRENT:
- Stage 1 داشبورد مرجع تکمیل شده.
- openPage/goHome تداخل‌زدایی شده.
- ordersPage ساخته شده.
- appData.orders و appData.contracts اضافه شده.
- ثبت/نمایش/لغو سفارش و قرارداد پیاده شده.
- کارت ثبت سفارش داشبورد به ordersPage وصل شده.
- renderAll به rhRenderRecords وصل شده.
- Syntax منطق سفارش/قرارداد تست و سالم است.
- Backup: www/index.html.backup-orders-contract
- Storage: localStorage / rahhesab_v2

ORDERS:
- مستقل از قرارداد.
- ثبت + وضعیت + لغو.
- چرخه نهایی سفارش باید قابل توسعه باشد: جدید، تخصیص، درحال‌انجام، تحویل‌شده، لغوشده.

CONTRACTS:
- مستقل از سفارش.
- ثبت + وضعیت + لغو.
- قرارداد شرکت پس از لغو باید در معماری نهایی دسترسی کاربران/رانندگان وابسته را غیرفعال کند.

ABC:
VIP + Bazaar + Support + Charity + Campaign + سایر قابلیت‌های توافق‌شده.
ABC نام بسته قابلیت‌هاست، نه یک صفحه مستقل.

VIP FINAL:
- 1 ماهه: 100000 تومان
- 2 ماهه: 200000 تومان
- فعال‌سازی بار و حمل 1 ماهه: 100000 تومان
- پرداخت فقط کافه‌بازار
- کارت‌به‌کارت/پرداخت مستقیم داخل برنامه ممنوع

CHABOK:
پنل مستقل راننده داخل راه‌حساب؛ جایگزین اپ اصلی نشود.

BUILD:
versionCode=2
versionName=1.1
targetSdk=36
AGP=8.9.1
Kotlin=2.1.20

FINAL GOAL:
راه‌حساب یک اپ کامل مالی و عملیاتی برای رانندگان آزاد، قراردادی، پیک، بار و شرکت‌ها باشد؛ شامل حسابداری، سفارش، قرارداد، مأموریت، راننده، شرکت، خودرو، VIP، پشتیبانی، خیریه، کمپین و پنل چابک.

NEXT:
1. تست واقعی ordersPage داخل APK
2. تکمیل وضعیت‌های سفارش و قرارداد
3. اتصال سفارش به مأموریت/راننده
4. منطق لغو قرارداد و غیرفعال‌سازی وابستگان
5. تکمیل ABC/VIP/Bazaar
6. تست کامل
7. build APK
8. commit/push
